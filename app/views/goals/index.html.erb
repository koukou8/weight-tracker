<div class="bg-white shadow rounded-lg p-6">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-800">目標設定</h1>
    <%= link_to "新規目標", new_goal_path, class: "bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded" %>
  </div>

  <% if @goals.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @goals.each do |goal| %>
        <div class="bg-white border rounded-lg shadow-sm p-6">
          <div class="flex justify-between items-center mb-4">
            <h2 class="text-lg font-semibold text-gray-800">目標体重: <%= goal.target_weight %> kg</h2>
            <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded-full">
              残り <%= goal.days_remaining %> 日
            </span>
          </div>
          
          <div class="mb-4">
            <p class="text-sm text-gray-600">目標日: <%= goal.target_date.strftime('%Y/%m/%d') %></p>
          </div>
          
          <div class="mb-4">
            <p class="text-sm text-gray-600 mb-1">進捗状況: <%= goal.progress_percentage %>%</p>
            <div class="w-full bg-gray-200 rounded-full h-2.5">
              <div class="bg-blue-600 h-2.5 rounded-full" style="width: <%= goal.progress_percentage %>%"></div>
            </div>
          </div>
          
          <div class="flex space-x-2">
            <%= link_to "詳細", goal_path(goal), class: "text-blue-600 hover:text-blue-900 text-sm" %>
            <%= link_to "編集", edit_goal_path(goal), class: "text-green-600 hover:text-green-900 text-sm" %>
            <%= link_to "削除", goal_path(goal), data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' }, class: "text-red-600 hover:text-red-900 text-sm" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="bg-gray-50 p-6 text-center rounded-lg">
      <p class="text-gray-500">目標設定がありません。「新規目標」から目標を追加してください。</p>
    </div>
  <% end %>
</div>
